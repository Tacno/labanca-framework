<?php

require "active_records/ActiveRecord.php";
require "models/Form.php";
require "models/La.php";
require "models/Model.php";
require "helpers/Dialog.php";

/*
	$folders = array('models','helpers');

	foreach($folders as $f)
	{
		foreach (glob($f."/*.php") as $filename)
		{
			require $filename;
			echo $filename . PHP_EOL;
		}
	}
	*/
?>
